package com.dtbx.springsecurity;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class EasyBankBackendApplicationTests {

    /**
     * Tests if the spring boot application context can be loaded.
     */
    @Test
    void contextLoads() {
        // This test is required to ensure that the Spring Boot application can be loaded
        // without any errors. The test itself is empty, but it is required to satisfy the
        // JUnit test framework.
    }

}
